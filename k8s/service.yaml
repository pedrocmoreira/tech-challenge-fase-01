apiVersion: v1
kind: Service
metadata:
  name: garage-system
  labels:
    app: garage-system
spec:
  type: ClusterIP
  selector:
    app: garage-system
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP