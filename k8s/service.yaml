apiVersion: v1
kind: Service
metadata:
  name: garage-system
  labels:
    app: garage-system
spec:
  type: NodePort
  selector:
    app: garage-system
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP